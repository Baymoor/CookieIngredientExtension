{
  "name": "cookie-ingredient-extension",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && tsc -p tsconfig.build.json && npx bestzip extension.zip dist/*",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.31",
    "@types/node": "^24.10.1",
    "bestzip": "^2.2.1",
    "typescript": "~5.9.3",
    "vite": "^7.2.2"
  }
}
